import SystemLayout from "@/components/system-layout";

export default function SystemLevelDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <SystemLayout>{children}</SystemLayout>;
} 